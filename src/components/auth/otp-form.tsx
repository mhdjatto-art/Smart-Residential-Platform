/**
 * Legacy OTP form — kept as an empty module for git history.
 *
 * The project switched from OTP to email+password auth in task #22.
 * The previous implementation referenced `verifyOtpSchema` which no longer
 * exists. Rather than maintain dead code, we leave this file as a stub
 * so any stale imports (none currently in the tree) fail loudly.
 */
export {};

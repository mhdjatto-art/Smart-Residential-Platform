package com.levant.srp;

import com.getcapacitor.BridgeActivity;

public class MainActivity extends BridgeActivity {
    // Empty body — all behaviour is inherited from BridgeActivity.
    // Capacitor reads capacitor.config.json from assets at runtime.
}

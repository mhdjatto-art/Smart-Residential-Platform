// With server.url set in capacitor.config.ts, Capacitor loads the live
// site directly. This file is only here as a safety net.
console.log("[LSRP] shell ready — Capacitor will navigate to server.url");
